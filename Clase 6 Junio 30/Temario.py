# BUILT-IN FUNCTIONS
# hash, len, locals, map, max, memory view
# min, next, object, oct, open, pow, range, 
# repr, reversed, round, set, slice, sorted
# string, sum

print(hash(2))

#Print(len"hola")

print(list(range(10)))
